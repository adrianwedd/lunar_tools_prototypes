# NOTE: `pip-compile --extra mlx --extra hw --extra cloud --extra tracing --extra dev
# --output-file=requirements.txt pyproject.toml` fails on this environment's Python 3.14
# (numba, a transitive dependency pulled in via mlx-whisper/librosa, fails to build a wheel
# under 3.14). This file was therefore updated BY HAND to match pyproject.toml's
# dependencies + all optional extras, unpinned. Regenerate with pip-compile once a
# Python <3.14 (e.g. 3.12) toolchain is available, or once numba ships 3.14 wheels.
#
# Base
ollama
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-logging
python-dotenv>=1.2.2
librosa
pydub
requests>=2.33.0
httpx
numpy
opencv-python
toml
pillow

# extra: vision
opencv-python>=4.10,<5

# extra: audio
soundfile

# extra: cloud
openai>=1.40
anthropic>=0.40.0
replicate>=0.30

# extra: tracing
langsmith>=0.1

# extra: mlx
mlx>=0.26
mlx-whisper>=0.4
faster-whisper>=1.0
mlx-lm>=0.24
mflux>=0.6

# extra: hw
sounddevice>=0.5
pyglet>=2.0
mido>=1.3
python-rtmidi>=1.5
pyzmq>=26
pynput>=1.7

# extra: dev
pytest>=9.0.3
pytest-mock>=3.14
pytest-cov>=4
responses>=0.25
pip-tools
pre-commit
bandit
detect-secrets

# Security floors for transitive dependencies (Dependabot alerts, 2026-07)
urllib3>=2.7.0
idna>=3.15
protobuf>=5.29.6
msgpack>=1.2.1
