> Correction 2026-07-10: the ".env tracked in git" blocker was refuted — .env was never committed (verified via git log --all -- .env).

# Hermes QA Report: lunar_tools_prototypes
**Date:** 2026-06-21 | **Mode:** Read-only audit

## Executive Summary
Lunar Tools Prototypes — Python-based interactive audiovisual art installations using Lunar Tools framework. AI-driven text, audio synthesis, image generation, real-time MIDI/keyboard controls, dynamic visuals. 32 commits, last updated 2026-03-26. Has `.env` tracked (BLOCKER), `.ruff_cache/` and `.pytest_cache/` tracked, output MP3s tracked, and untracked test files.

**Overall Score: 4.5 / 10**

## Dimensions
1. Research Integrity: 5 — Gemini tasks.yml, Codex review. Specs and plans in docs/. Gallery site spec. But no formal research.
2. Strategic Coherence: 6 — CLAUDE.md defines project. .gemini/GEMINI.md. .pre-commit-config.yaml. CI workflow. But scope is broad (art installations + hackathon demo + gallery site).
3. Content-to-Research Integration: 5 — Specs and plans exist. Codex review referenced. But prototypes may have diverged from specs.
4. Technical Architecture: 5 — Python package structure (src/lunar_tools_art/). CI (ci.yml). Pre-commit (ruff, bandit). .env.example present. But `.env` tracked, caches tracked, output files tracked.
5. Security: 3 — `.env` file tracked in git (BLOCKER — contains API keys). `.claude/claude.log` tracked (may contain sensitive info). `.claude/settings.local.json` tracked.
6. Data Model: 5 — Prototypes, prosody module, tools, prototype_base. Output files (mp3, txt). But output directory tracked with generated files.
7. UX: 5 — Art installations (audio mirror, etc.). Gallery site for GitHub Pages. Hackathon demo. No standard UI — installation-specific.
8. Code Quality: 5 — Python 3.10+ syntax (`str | None`, `list[str]`). Pre-commit with ruff. pytest tests. But untracked test file, modified source files, `.ruff_cache/` tracked.
9. Documentation Coherence: 6 — CLAUDE.md, GEMINI.md. Specs and plans. .env.example. CONTRIBUTING, issue templates. But `.env` tracked undermines docs.
10. Overall Readiness: 4 — 32 commits. Last commit Mar 2026. `.env` tracked is a blocker. Multiple cache directories tracked. Output files tracked. Modified working tree.

## Blockers
1. `.env` file tracked in git — contains API keys for AI services
2. `.claude/claude.log` tracked — may contain sensitive session data

## Gaps
- `.ruff_cache/` and `.pytest_cache/` tracked — should be gitignored
- `.output/` directory with generated MP3s tracked — should be gitignored
- `tests/test_ai_services.py` untracked
- Python 3.10+ syntax without `from __future__ import annotations`

## Tech Debt
- `.afterwords` = "reginald-perrin"
- `.claude/settings.local.json` tracked
- Modified source files not committed
- `.bandit` config file tracked (good practice)

## Scorecard
|1|5| 2|6| 3|5| 4|5| 5|3| 6|5| 7|5| 8|5| 9|6| 10|4| **Avg**|**4.5**|
